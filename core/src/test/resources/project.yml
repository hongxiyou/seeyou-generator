# Maven 项目结构配置
groupId: org.seeyou.demo
artifactId: seeyou-demo
version: 0.1.0-SNAPSHOT
name: SeeYou Demo

modules:
  server:
    artifactId: ${artifactId}-server
    name: ${name} -- Server
  
  client:
    artifactId: ${artifactId}-client
    name: ${name} -- Client
  
  frontend:
    artifactId: ${artifactId}-frontend
    name: ${name} -- Frontend
